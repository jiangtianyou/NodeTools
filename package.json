{
  "name": "my-token-gen",
  "version": "1.0.4",
  "scripts": {
    "pub": "git add -A && git commit -m '修改' && npm version patch && npm publish",
    "push":"git add -A && git commit -m '修改' && git push -u origin master"
  },
  "dependencies": {
    "jsonwebtoken": "^8.5.1"
  },
  "bin": {
    "jwt": "./bin/index.js"
  }
}
